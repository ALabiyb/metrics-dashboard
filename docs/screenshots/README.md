# Screenshots

Place PNG screenshots here and reference them from the main `README.md`.

Suggested set:

| Filename | Content |
|---|---|
| `overview.png` | Home page — Mission Control layout with KPI tiles, ring gauges, node grid |
| `nodes.png` | 5×2 grid of per-node cards (CPU/MEM ring gauges, pods, net, disk) |
| `ceph.png` | Ceph panel with health/OSD/IOPS |
| `alerts.png` | Rolling alerts/events feed |
| `login-oidc.png` | Login page with SoftNet AD SSO |
| `architecture.png` | Architecture diagram (Eraser export) — **already present** |

**Tips:**
- Browser at 1920×1080 or 1440×900 for readable text
- Prefer real data over mock; the simulator has a "SIM" badge
- Crop out OS chrome
- Compress with `pngquant`
