// app.jsx — mounts the dashboard full-screen (no design-canvas wrapper; that
// was only needed to compare the two prototype directions side by side).
// Globals: React, ReactDOM, Dashboard.
//
// Author: Labiyb M. Said — DevSecOps Engineer
// Contact: abdulmunimsaid82@gmail.com

ReactDOM.createRoot(document.getElementById('root')).render(<Dashboard />);
